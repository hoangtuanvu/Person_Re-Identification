from setuptools import find_packages, setup

setup(
    name='trt_models',
    version='0.0',
    description='TensorFlow models accelerated with NVIDIA TensorRT',
    author='',
    author_email='',
    url='https://github.com/NVIDIA-Jetson/trt_models',
    packages=find_packages(),
)
